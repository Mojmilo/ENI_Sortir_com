{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}

<h2 class="mb-4 text-4xl tracking-tight font-extrabold text-center text-gray-900 dark:text-white">Mon profil</h2>

{# todo: charger la photo depuis la bdd #}
<img class="w-24 h-24 mb-3 mx-auto rounded-full shadow-lg" 
src="images/jeune-bel-homme-isole-dans-poses-differentes-fond-blanc-illustration_632498-859.avif" 
alt="picture profil"/>

<div class="max-w-sm mx-auto">




    <div class="mb-5">
        <p class="mb-3 text-gray-500 dark:text-gray-400">
            <strong>Pseudo :</strong> {{ user.pseudo }}
        </p>
    </div>

    <div class="mb-5">
        <p class="mb-3 text-gray-500 dark:text-gray-400">
            <strong>Prénom :</strong> {{ user.firstname }}
        </p>
    </div>

    <div class="mb-5">
        <p class="mb-3 text-gray-500 dark:text-gray-400">
            <strong>Nom :</strong> {{ user.lastname }}
        </p>
    </div>

    <div class="mb-5">
        <p class="mb-3 text-gray-500 dark:text-gray-400">
            <strong>Téléphone :</strong> {{ user.phone }}
        </p>
    </div>

    <div class="mb-5">
        <p class="mb-3 text-gray-500 dark:text-gray-400">
            <strong>Email :</strong> {{ user.email }}
        </p>
    </div>

    <div class="mb-5">
        <p class="mb-3 text-gray-500 dark:text-gray-400">
            <strong>Ville de rattachement :</strong> {{ user.site.name }}
        </p>
    </div>

    <div class="mt-6 flex items-center justify-center gap-x-6">
        {% if app.user.pseudo == user.pseudo %}
        <a href="{{ path('app_user_id_update', {id: user.id}) }}" alt=""><button type="button" class="focus:outline-none text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-900">
        Modifier mon mot de passe</button></a>
        {% endif %}
        {% if is_granted("ROLE_ADMIN") or app.user.pseudo == user.pseudo %}
            {# todo: faire un modal pour vérifier si on est sûr de vouloir supprimer l'utilisateur #}
            <a href="{{ path('app_user_id_delete', {id: user.id}) }}" alt="" ><button type="button" class="focus:outline-none text-white bg-red-700 hover:bg-red-800 focus:ring-4 focus:ring-red-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:bg-red-600 dark:hover:bg-red-700 dark:focus:ring-red-900">
            Supprimer</button></a>
        {% endif %}
        {% if is_granted("ROLE_ADMIN") or app.user.pseudo == user.pseudo %}
        <a href="{{ path('app_user_id_update', {id: user.id}) }}" alt=""><button type="button" class="focus:outline-none text-black bg-yellow-400 hover:bg-yellow-500 focus:ring-4 focus:ring-yellow-300 font-medium rounded-lg text-sm px-5 py-2.5 me-2 mb-2 dark:focus:ring-yellow-900">
        Modifier</button></a>
        {% endif %}
    </div>



</div>

{% endblock %}
